name: Release

on:
  push:
    branches:
      - master
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            task: distUnix
            artifact: "cli/build/dist/melo-*-linux.tar.gz"
            label: linux
          - os: macos-latest
            task: distUnix
            artifact: "cli/build/dist/melo-*-macos.tar.gz"
            label: macos
          - os: windows-latest
            task: distWindows
            artifact: "cli/build/dist/melo-*-windows.zip"
            label: windows

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6.0.2

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '25'
          distribution: 'graalvm'

      - name: Build distribution
        run: ./gradlew :cli:${{ matrix.task }}

      - name: Upload artifact
        uses: actions/upload-artifact@v7.0.0
        with:
          name: melo-${{ matrix.label }}
          path: ${{ matrix.artifact }}

      - name: Upload native image
        uses: actions/upload-artifact@v7.0.0
        with:
          name: melo-native-${{ matrix.label }}
          path: cli/build/native/nativeCompile/${{ matrix.os == 'windows-latest' && 'melo.exe' || 'melo' }}

  release:
    needs: build
    runs-on: ubuntu-latest
    # Only create a GitHub Release when a tag is pushed
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8.0.0
        with:
          path: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
