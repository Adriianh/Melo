#!/usr/bin/env sh
MELO_HOME="$(cd "$(dirname "$0")/.." && pwd)"
if [ "$(uname)" = "Darwin" ]; then
  export DYLD_LIBRARY_PATH="$MELO_HOME:$DYLD_LIBRARY_PATH"
else
  export LD_LIBRARY_PATH="$MELO_HOME:$LD_LIBRARY_PATH"
fi
exec "$MELO_HOME/melo" "$@"
