#!/usr/bin/env sh
MELO_HOME="$(cd "$(dirname "$0")/.." && pwd)"
exec java \
  --enable-native-access=ALL-UNNAMED \
  -XX:+UseSerialGC \
  -Xms16m \
  -Xmx256m \
  -XX:TieredStopAtLevel=1 \
  -jar "$MELO_HOME/melo.jar" "$@"
